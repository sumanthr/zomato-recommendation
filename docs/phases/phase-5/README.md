# Phase 5 Resources (React Frontend)

- Frontend app: `src/phases/phase_5/frontend/`
- API base URL env: `VITE_API_BASE_URL` (default `http://127.0.0.1:8000`)

Run:

```bash
cd src/phases/phase_5/frontend
npm install
npm run dev
```
